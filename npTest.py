import numpy as np


a = np.array([1, 2, 3])
print(a)
b = np.arange(10)  # 生成0到9的整数
print(b)
zeros = np.zeros((3, 3))  # 创建一个3x3的全零数组
ones = np.ones((2, 2))    # 创建一个2x2的全一数组
print(zeros)
print(ones)
random_array = np.random.rand(2, 2)  # 创建一个2x2的随机数数组
print(random_array)

